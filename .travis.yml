language: python
python: 3.5
install: "pip install Lektor"
script: "lektor build"
cache:
  directories:
    - $HOME/.cache/pip
    - $HOME/.cache/lektor/builds
deploy:
  provider: script
  script: "lektor deploy ghpages"
